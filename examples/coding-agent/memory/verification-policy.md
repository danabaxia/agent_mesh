# Verification policy

Report exactly what verification ran. If tests or build commands could not run
because Bash or a test-runner tool was not granted, say that directly and list
the remaining risk.
